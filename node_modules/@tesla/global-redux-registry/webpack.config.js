const path = require('path');

module.exports = {
    devtool: "#source-map",
    mode: 'development',    
    entry: {
        'tesla-redux-registry': ['./src/index.js']
    },
    output: {
        path: path.resolve(__dirname, 'dist/js'),
        filename: './[name].js'
    },
    module: {
        rules: [{
            test: /\.js$/,
            exclude: /(node_modules)/,
            use: {
                loader: 'babel-loader',
                options: {
                	presets: ['@babel/preset-env'],
                    plugins: [require('@babel/plugin-proposal-object-rest-spread')]
                }
            }
        }]
    }
};