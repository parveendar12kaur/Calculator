const validator = require('./validator');
const transformer = require('./transformer');
const migration = require('./migration');
const TYPES = require('./utils/types');
const lexicon = require('./utils/lexicon');
const utils = require('./utils/configuration');
const matching = require('./matching');
const upgrade = require('./upgrade');
const npmPackage = require('../package.json');

module.exports = {
    version: npmPackage.version,
    matching: matching.matching,
    toggleOption: validator.toggleOption,
    toggleOptionByGroup: validator.toggleOptionByGroup,
    toggleOptions: validator.toggleOptions,
    validateRules: validator.validateRules,
    validatePrice: validator.validatePrice,
    validatePriceWithSubtotal: validator.validatePriceWithSubtotal,
    validateRulesAndPrice: validator.validateRulesAndPrice,
    validateRulesAndPriceWithSubtotal: validator.validateRulesAndPriceWithSubtotal,
    validate: validator.validate,
    validatePromise: validator.validatePromise,
    validateObservable$: validator.validateObservable$,
    getSubTotal: utils.getSubTotal,
    getMarketingConfiguration: utils.getMarketingConfiguration,
    getManufacturingConfiguration: utils.getManufacturingConfiguration,
    mapOptions: transformer.mapOptions,
    mapConfiguration: transformer.transformWithRules,
    transform: transformer.transform,
    transformPromise: transformer.transformPromise,
    transformObservable$: transformer.transformObservable$,
    getAllPossibleConfiguration: utils.getAllPossibleConfiguration,
    migrate: migration.migrate,
    Lexicon: lexicon,
    upgrade: upgrade.upgrade,
    TYPES
};
