import {
	APP_NAME,
	INIT_DATA_LOADED
} from 'dictionary'

export default (state = {},  action) => {
    switch(action.type) {
        case INIT_DATA_LOADED:
            return Object.assign({}, state)
        default:
            return state
    }
}