import React,{Component} from 'react'
import PropTypes from 'prop-types'

export default class ComponentContainer extends Component {

	constructor(props){

	}

	render(){
		const {className, componentName} = this.props
        <div data-component={componentName} className={``}>

        </div>
	}
}

ComponentContainer.defaultProps = {
	componentName: PropTypes.string,
	className: 'tsla--dashboard-container'
}

ComponentContainer.propTypes = {
	componentName: PropTypes.string.isRequired,
	className: PropTypes.string
}