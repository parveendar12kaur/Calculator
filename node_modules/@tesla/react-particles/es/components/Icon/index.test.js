'use strict';

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _enzyme = require('enzyme');

var _ = require('./');

var _2 = _interopRequireDefault(_);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('<Icon />', function () {
  it('should render without throwing an error', function () {
    Object.keys(_.IconTypes).forEach(function (v) {
      var type = _.IconTypes[v];
      expect((0, _enzyme.shallow)(_react2.default.createElement(_2.default, { type: type, size: 'small' })).prop('className').split(' ').length).toBe(2);
    });
  });

  it('should support different sizes', function () {
    Object.keys(_.IconSizes).forEach(function (v) {
      var size = _.IconSizes[v];
      expect((0, _enzyme.shallow)(_react2.default.createElement(_2.default, { type: 'calendar', size: size })).prop('className').split(' ').length).toBe(2);
    });
  });
}); /* global describe, it, expect */